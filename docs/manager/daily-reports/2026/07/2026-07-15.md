## Overview

- Date: 2026-07-15
- Branch: main
- Author: AI agents (Claude Code), session-driven, reviewed by Guimaraes Mahota

## Completed

- Redesigned the `/now` immersive feed end-to-end to match the hifi design prototype (`docs/cto/task/1. V01 - New Layout/designs/Agora - Bandeja.dc.html`):
  - New `GET /api/categories` route.
  - New `lib/immersive/bandeja-store.ts` (personalization store) and `lib/immersive/pace.ts` (reading pace definitions).
  - New `components/immersive/onboarding-bandeja.tsx` (first-visit picker) and `components/immersive/bandeja-overlay.tsx` (category picker overlay).
  - Redesigned `story-card.tsx`, `story-feed.tsx`, `immersive-nav.tsx`, `story-context-panel.tsx`.
  - Added dormant `spatialAsset` scaffolding to `components/immersive/types.ts` and built (but unused-in-production) `components/immersive/spatial-story-viewer.tsx`.

## Decisions

Recorded in `docs/memory/decisions.md` (2026-07-15 entry — "`/now` immersive feed redesign"):
- Personalization state via hand-rolled `useSyncExternalStore` singleton, no new state-management dependency.
- Category filter switch re-fetches from the real paginated API rather than client-side reordering.
- No-fabricated-data policy: dropped/adapted/deferred fields from the design prototype that don't exist in the real schema.
- `spatialAsset` added as optional, intentionally dormant scaffolding pending future schema/cron work.
- Explorar mural redesign and schema/migration work explicitly out of scope this pass.

Also recorded in `docs/memory/ui-standards.md` (useSyncExternalStore pattern note).

## Files Changed / Areas

- `app/api/categories/route.ts` (new)
- `lib/immersive/bandeja-store.ts` (new)
- `lib/immersive/pace.ts` (new)
- `components/immersive/onboarding-bandeja.tsx` (new)
- `components/immersive/bandeja-overlay.tsx` (new)
- `components/immersive/story-card.tsx`, `story-feed.tsx`, `immersive-nav.tsx`, `story-context-panel.tsx`, `spatial-story-viewer.tsx`, `types.ts`

## Validation Run

- `pnpm typecheck` — clean relative to pre-existing baseline (5 unrelated pre-existing errors in `share-panel.tsx`/`experiment-results.ts`/`experiments.ts`, not introduced or fixed here).
- `pnpm lint` — clean relative to pre-existing baseline (1 unrelated pre-existing warning).
- `pnpm build` — fails with a `WasmHash`/webpack error; confirmed via `git stash` this reproduces identically on unmodified `main` (pre-existing Node v24.17.0 + webpack incompatibility, not a regression from this work).
- Manual/live browser exercise: onboarding flow, category selection persistence, bandeja overlay open/select/restore, category-filter refetch, why-popover reasons, context panel real-data rendering — all confirmed working.
- No automated test suite covers this area — not claiming coverage exists.

## Risks / Blockers

- `pnpm build` is broken on this machine independent of this change (pre-existing, see Validation Run) — worth its own investigation/roadmap item if not already tracked.
- Deferred backend work (`spatialAsset`, real multi-source/facts data model) is schema-affecting (High classification per `CLAUDE.md`) and needs its own task-spec before implementation.

## Next Actions

- Track backend support for `spatialAsset`/facts/multi-source as its own task (roadmap #22).
- Track "Explorar" mural redesign as its own task (roadmap #23).
- Consider a testing pass (roadmap item, `09-testing`) for the new immersive feed flows — currently uncovered by automated tests.
